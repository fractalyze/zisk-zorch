# zisk-zorch runtime dependencies.
#
# Pulled from the Fractalyze package index alongside upstream PyPI:
#   pip install -r requirements.in \
#     --extra-index-url https://fractalyze.github.io/pypi/simple/
#
# zisk-zorch is the ZisK-specific consumer of zorch's agnostic blocks; zorch
# itself arrives as a Bazel module (see MODULE.bazel), not a pip dep.
# zk_dtypes (native finite-field dtypes) arrives transitively via jax and is
# used directly on the pil2 byte-match path.
absl-py
numpy
jax==0.0.5.dev20260606003451
jaxlib==0.0.5.dev20260606003451
jax-cuda12-plugin==0.0.5.dev20260524000415
zkx-cuda-pjrt==0.0.5.dev20260606003451
