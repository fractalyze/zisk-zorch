# Bazel
/bazel-*
/.ijwb/
/.aswb/
/.clwb/

# Local, machine-specific Bazel config (never commit — holds absolute paths)
.bazelrc.user

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtualenvs (dev envs live under $DEVENV_ENVS_DIR, but ignore in-tree ones too)
.venv/
venv/

# Rust fixture generator build output (tools/fixture-gen/.gitignore also
# carries /target so the crate stays self-contained, as sp1-zorch's does)
/tools/fixture-gen/target/

# Compiled / vendored shared libs are opt-in only
*.so
*.dylib
*.dll

# Editors / OS
.vscode/
.idea/
.DS_Store
